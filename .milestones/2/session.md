# tmux `amaru` — M2 routine Amaru fault injection — swept 2026-09-07T11:55:50Z

The milestone desk asks, answers and sweeps. Technical work stays in the epic
window and is controlled only through cna#205 epic owner `%432`. The exact
child-authored fragment copied to `resume/e205.md` has SHA-256
`6247939c67e08be37851ba8806db703657955a3af57308e737323b01f5247e5a`.

## `amaru:1 ms2-amaru-routine` — milestone singleton `%694`

Working directory: `/code/cardano-node-antithesis`.

Current observed process argv:

`/nix/store/nqhk0522q8ncygwwx054iq76ckcjll82-codex-0.153.2/libexec/codex --yolo resume 01a07b92-4209-7320-a060-717cc808f63c`

Portable replay from the exact working directory:

`codex --yolo resume 01a07b92-4209-7320-a060-717cc808f63c`

Runtime: `/tmp/ms-cardano-node-antithesis-2/`. Load
`.milestones/2/resume/ms.md`, then the milestone journal and snapshot. The
historical lane-operations handoff does not define this role.

## `amaru:2 cna-e205-lane` — cna#205 epic owner `%432`

Working directory: `/code/cardano-node-antithesis`.

Exact child-authored replay command:

`claude --dangerously-skip-permissions --model 'claude-opus-5[1m]' --effort high`

Runtime: `/tmp/ms-cardano-node-antithesis-2/e-auto/`. Resume from
`.milestones/2/resume/e205.md`, then read its own append-only `STATUS.md` and
`epic-map.md`. The epic owner owns its full child topology and scheduled-run
read duty. The M2 desk never addresses its ticket owner or workers directly.

## Standing reconstruction constraints

- This session has one singleton desk window and one epic-owned execution
  window. Pane `%308` is absent and the redundant lane-operations role is
  retired.
- No nightly rerun, manual fire, background polling, `/loop`, cron addition,
  merge, product edit or upstream human contact is authorised by recovery.
- A future lane seat receives its own runtime root and `STATUS.md`; no process
  other than `%432` writes the epic owner's journal.
- Copy child-authored resume fragments byte-for-byte and verify their hashes.
  Never reconstruct a child launch command from memory or a stale snapshot.
